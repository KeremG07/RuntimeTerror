package domain.body.fallingBody;

public class Box extends FallingBody{
    public Box(int x_coordinates,
                         int y_coordinates,
                         int length,
                         int width){
        super(x_coordinates, y_coordinates, length, width);
    }
}
